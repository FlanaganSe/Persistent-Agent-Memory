[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ci-test-app"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
ci-app = "app.main:run"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
target-version = "py312"
