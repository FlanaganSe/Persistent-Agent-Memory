[project]
name = "myproject"
version = "0.1.0"
requires-python = ">=3.12"

[project.scripts]
myproject = "myproject:main"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
target-version = "py312"
