# Contributing

Use the contributor guide in [docs/contributing.md](docs/contributing.md).

Short version:

```bash
uv pip install ".[dev]"
uv run nox -s ci
```

Preserve the trust-boundary invariants, add regression tests for behavior changes, and keep docs aligned with real output paths and maturity.
